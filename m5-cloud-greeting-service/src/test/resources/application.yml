server:
  port: 30201

spring:
  application:
    name: greeting-service-test
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:30800/eureka/
  instance:
    lease-expiration-duration-in-seconds: 2
    lease-renewal-interval-in-seconds: 1
feign:
  hystrix:
    enabled: true
#logging:
#  level:
#    root: debug
greeting-service.ribbon.ServerListRefreshInterval: 1000




